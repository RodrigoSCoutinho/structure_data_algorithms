// utilizando o método concat para concatenar arrays

const positiveNumbers = [1, 2, 3];
const negativeNumbers = [-3, -2, -1];

const numbers = negativeNumbers.concat(positiveNumbers);

console.log(numbers); // [-3, -2, -1, 1, 2, 3]